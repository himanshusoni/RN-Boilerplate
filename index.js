import App from './app/index'

/*
 * uncomment below line and run `yarn storybook` to start storybook
 * TODO find a way to easily switch between app and storybook in dev mode.
 * may use __dev__ && __storyies__
*/
// import Storybook from './app/storybook'
